// Supabase の API Keys 画面から、この2か所だけ書き換えてま。
// sb_secret_... は絶対に入れないでください。
// Publishable key (sb_publishable_...) を使います。

window.BABYMA_CONFIG = {
  SUPABASE_URL: "ここにProject URLを貼る",
  SUPABASE_PUBLISHABLE_KEY: "ここにPublishable keyを貼る"
};
